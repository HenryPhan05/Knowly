KnowLy
app: working with these tabs (icon using MaterialIcon)
assess/images: the images or photo 
components: add the UI that we can reuse(DarkTheme, AuthContext)
lib: use for database
styles/themes: store the values CSS